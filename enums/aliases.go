package enums

// Type aliases for common enum types for convenience.

type String = Enum[string]

type Int = Enum[int]

type Int32 = Enum[int32]

type Int64 = Enum[int64]

type Float32 = Enum[float32]

type Float64 = Enum[float64]

type Uint = Enum[uint]

type Uint8 = Enum[uint8]

type Uint16 = Enum[uint16]

type Uint32 = Enum[uint32]

type Uint64 = Enum[uint64]

type Bool = Enum[bool]
